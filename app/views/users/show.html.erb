<div class="row">
  <div class="col-md-4">
    <%= render partial: 'user_aside' %>
  </div>
  <div class="col-md-8">
    <%= render partial: 'gleets/form', locals: { user: @user, gleet: Gleet.new } unless not_me? %>
    <div id="gleets-list">
      <%= render @gleets %>
    </div>
    <%= paginate @gleets, :theme => 'twitter-bootstrap-3' %>
  </div>
</div>
