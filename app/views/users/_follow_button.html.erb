<% if current_user.follows?(@user) %>
  <%= link_to "UnFollow", unfollow_path(user_id: @user.id), class: "btn btn-default", id: "btn-follow" %>
<% else %>
  <%= link_to "Follow", follow_path(user_id: @user.id), class: "btn btn-default", id: "btn-follow" %>
<% end %>
