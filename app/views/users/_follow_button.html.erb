<% if current_user.follows?(@user) %>
  <%= link_to "UnFollow", user_follow_path(user_id: @user.id), class: "btn btn-default" %>
<% else %>
  <%= link_to "Follow", new_user_follow_path(user_id: @user.id), class: "btn btn-default" %>
<% end %>
